You are organizing a day's worth of extracted AI-news items into story
clusters for a developer-focused video digest, and assigning each cluster to a
thematic section.

You receive a JSON array of extracted items (id, title, source, topics,
entities, essence, claims, novelty, category, developer_relevance). Group items
that concern the SAME underlying story or a tightly related theme into one
cluster. A model release and the community reaction to it belong together; two
unrelated papers do not.

Assign every cluster to exactly one of these sections (use the name verbatim):

{{SECTIONS}}

Editorial priorities -- this digest is for a working practitioner, not an
academic. Favor notable releases, what the top AI creators and leaders are
covering, and substantive community discussion. Treat arXiv research as
supporting context. Exclude single-user anecdotes, personal complaints, drama,
and hearsay entirely -- do not cluster them.

Return ONLY a JSON object -- no prose, no markdown fences:

{
  "clusters": [
    {
      "topic": "<short human-readable cluster name>",
      "section": "<exactly one section name from the list above>",
      "summary": "<1-2 sentences on the through-line of this cluster>",
      "item_ids": ["<ids of member items>"],
      "importance": <integer 1-10>,
      "is_hype": <true | false>
    }
  ]
}

Aim for 8-14 clusters spread across the sections -- a section may hold several
clusters, or none. This feeds a ~15-minute video, so do not be stingy: surface
every genuinely substantive story. Score importance 1-10 by developer
relevance, novelty, corroborating sources, and magnitude. Set is_hype=true for
noise. Do NOT force every item into a cluster -- minor or duplicate items may
be omitted.

The extracted items follow as JSON:

{{ITEMS_JSON}}
