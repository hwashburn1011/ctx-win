# ai-daily pipeline dependencies. Pinned for reproducibility.
# Install:  pip install -r requirements.txt
#
# Only the Stage 1 block is required to run ingest. The later-stage packages
# are listed now so the full environment is reproducible; faster-whisper,
# playwright and kokoro are large -- install them when you reach those stages
# if you want a lighter Stage 1 setup.

# --- Stage 1: ingest ---------------------------------------------------------
requests==2.32.3
feedparser==6.0.11
PyYAML==6.0.2
python-dotenv==1.0.1
rich==13.9.4
youtube-transcript-api==0.6.3
yt-dlp==2025.2.19            # update often -- YouTube changes break old builds

# --- Stage 1: transcription fallback (optional, large) ----------------------
faster-whisper==1.1.1

# --- Stages 2-4: LLM (Anthropic SDK) ----------------------------------------
anthropic==0.45.2

# --- Stage 5: voice (Kokoro TTS, local) -------------------------------------
kokoro-onnx==0.5.0
soundfile==0.13.1

# --- Stage 6: visuals -------------------------------------------------------
playwright==1.59.0          # also run: playwright install chromium
Pillow==12.2.0

# --- Stage 7: assembly (ffmpeg is called via subprocess; moviepy optional) --
moviepy==2.1.2

# --- Development ------------------------------------------------------------
pytest==8.3.4
